# Interlude backend
# Unpinned on purpose: uv.lock is what makes builds reproducible.

# --- API ---
fastapi
uvicorn[standard]
python-multipart          # video uploads
sse-starlette             # progress stream for the live timeline UI
pydantic-settings         # env config
python-dotenv

# --- B2 storage (S3-compatible) ---
boto3                     # presigned URLs, multipart, object lock, versioning

# --- HTTP ---
httpx

# --- Generation ---
# genblaze 0.4.4 is an umbrella: pulls genblaze-core + genblaze-s3.
# Repo: github.com/backblaze-labs/genblaze
genblaze

# Provider adapters. Writer and checker must be DIFFERENT providers,
# so both openai and google are here on purpose.
genblaze-openai
genblaze-google
genblaze-elevenlabs

# Transcription: no STT adapter is listed in the Genblaze provider set.
# Verify before assuming. If there's no STT modality, call Whisper directly
# and say so plainly in the write-up rather than pretending otherwise.

# --- Media ---
# ffmpeg and ffprobe are called as subprocesses, not via a Python wrapper.
# Nothing to install here, but they must exist on PATH locally and in the
# deploy image. Check with: ffprobe -version

# --- Dev ---
# ruff
# pytest
